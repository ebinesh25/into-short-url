version: '3.8'

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: intolink-api
    ports:
      - "8080:8080"
    env_file:
      - ./api/.env
    environment:
      - PORT=8080
    restart: always
